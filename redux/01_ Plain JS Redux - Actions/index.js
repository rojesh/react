const redux = require("redux") // redux is mostly in JS that's why we use require

const action = {
    type: "ADD_"// types are upper_snake_case
    // actions are just a type property with actions that we want to make
}